/*
 * Trieve API
 * Trieve OpenAPI Specification. This document describes all of the operations available through the Trieve API.
 *
 * The version of the OpenAPI document: 0.5.0
 * Contact: developers@trieve.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.UUID;
import org.openapitools.client.model.ChunkData;
import org.openapitools.jackson.nullable.JsonNullable;
import javax.validation.constraints.*;
import javax.validation.Valid;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-03-29T12:28:22.088024700-05:00[America/Chicago]", comments = "Generator version: 7.4.0")
public class CreateChunkData extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CreateChunkData.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CreateChunkData.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CreateChunkData' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ChunkData> adapterChunkData = gson.getDelegateAdapter(this, TypeToken.get(ChunkData.class));

            final Type typeInstance = new TypeToken<List<ChunkData>>(){}.getType();
            final TypeAdapter<List<@Valid ChunkData>> adapterChunkDataList = (TypeAdapter<List<ChunkData>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstance));

            return (TypeAdapter<T>) new TypeAdapter<CreateChunkData>() {
                @Override
                public void write(JsonWriter out, CreateChunkData value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `ChunkData`
                    if (value.getActualInstance() instanceof ChunkData) {
                      JsonElement element = adapterChunkData.toJsonTree((ChunkData)value.getActualInstance());
                      elementAdapter.write(out, element);
                      return;
                    }
                    // check if the actual instance is of the type `List<@Valid ChunkData>`
                    if (value.getActualInstance() instanceof List<?>) {
                      List<?> list = (List<?>) value.getActualInstance();
        	            if(list.get(0) instanceof ChunkData) {
        		            JsonArray array = adapterChunkDataList.toJsonTree((List<@Valid ChunkData>)value.getActualInstance()).getAsJsonArray();
                        elementAdapter.write(out, array);
                        return;
        	            }
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: ChunkData, List<@Valid ChunkData>");
                }

                @Override
                public CreateChunkData read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize ChunkData
                    try {
                      // validate the JSON object to see if any exception is thrown
                      ChunkData.validateJsonElement(jsonElement);
                      actualAdapter = adapterChunkData;
                      match++;
                      log.log(Level.FINER, "Input data matches schema 'ChunkData'");
                    } catch (Exception e) {
                      // deserialization failed, continue
                      errorMessages.add(String.format("Deserialization for ChunkData failed with `%s`.", e.getMessage()));
                      log.log(Level.FINER, "Input data does not match schema 'ChunkData'", e);
                    }
                    // deserialize List<@Valid ChunkData>
                    try {
                      // validate the JSON object to see if any exception is thrown
                      if (!jsonElement.isJsonArray()) {
                        throw new IllegalArgumentException(String.format("Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
                      }

                      JsonArray array = jsonElement.getAsJsonArray();
                      // validate array items
                      for(JsonElement element : array) {
                        ChunkData.validateJsonElement(element);
                      }
                      actualAdapter = adapterChunkDataList;
                      match++;
                      log.log(Level.FINER, "Input data matches schema 'List<@Valid ChunkData>'");
                    } catch (Exception e) {
                      // deserialization failed, continue
                      errorMessages.add(String.format("Deserialization for List<@Valid ChunkData> failed with `%s`.", e.getMessage()));
                      log.log(Level.FINER, "Input data does not match schema 'List<@Valid ChunkData>'", e);
                    }

                    if (match == 1) {
                        CreateChunkData ret = new CreateChunkData();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CreateChunkData: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CreateChunkData() {
        super("oneOf", Boolean.FALSE);
    }

    public CreateChunkData(ChunkData o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public CreateChunkData(List<@Valid ChunkData> o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("ChunkData", ChunkData.class);
        schemas.put("List<@Valid ChunkData>", List.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CreateChunkData.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * ChunkData, List<@Valid ChunkData>
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof ChunkData) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof List<?>) {
            List<?> list = (List<?>) instance;
        	if(list.get(0) instanceof ChunkData) {
        		super.setActualInstance(instance);
        		return;
        	}
        }

        throw new RuntimeException("Invalid instance type. Must be ChunkData, List<@Valid ChunkData>");
    }

    /**
     * Get the actual instance, which can be the following:
     * ChunkData, List<@Valid ChunkData>
     *
     * @return The actual instance (ChunkData, List<@Valid ChunkData>)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `ChunkData`. If the actual instance is not `ChunkData`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ChunkData`
     * @throws ClassCastException if the instance is not `ChunkData`
     */
    public ChunkData getChunkData() throws ClassCastException {
        return (ChunkData)super.getActualInstance();
    }
    /**
     * Get the actual instance of `List<@Valid ChunkData>`. If the actual instance is not `List<@Valid ChunkData>`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `List<@Valid ChunkData>`
     * @throws ClassCastException if the instance is not `List<@Valid ChunkData>`
     */
    public List<@Valid ChunkData> getChunkDataList() throws ClassCastException {
        return (List<@Valid ChunkData>)super.getActualInstance();
    }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to CreateChunkData
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
    // validate oneOf schemas one by one
    int validCount = 0;
    ArrayList<String> errorMessages = new ArrayList<>();
    // validate the json string with ChunkData
    try {
      ChunkData.validateJsonElement(jsonElement);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for ChunkData failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    // validate the json string with List<@Valid ChunkData>
    try {
      if (!jsonElement.isJsonArray()) {
          throw new IllegalArgumentException(String.format("Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
      }
      JsonArray array = jsonElement.getAsJsonArray();
      // validate array items
      for(JsonElement element : array) {
        ChunkData.validateJsonElement(element);
      }
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for List<@Valid ChunkData> failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    if (validCount != 1) {
      throw new IOException(String.format("The JSON string is invalid for CreateChunkData with oneOf schemas: ChunkData, List<@Valid ChunkData>. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
    }
  }

 /**
  * Create an instance of CreateChunkData given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of CreateChunkData
  * @throws IOException if the JSON string is invalid with respect to CreateChunkData
  */
  public static CreateChunkData fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CreateChunkData.class);
  }

 /**
  * Convert an instance of CreateChunkData to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

